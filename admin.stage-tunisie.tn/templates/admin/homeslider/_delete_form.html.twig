<form method="post" action="{{ path('home_slider_delete', {'id': home_slider.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home_slider.id) }}">
    <button class="btn">Delete</button>
</form>
